mixin video(video)
    a(href=`videos/${video.id}`).video_mixin
        div.video_mixin__thumb
        div.video_mixin__data
            span.video_mixin__title=video.title
            div.video_mixin__meta
                span 🙍🏻 #{video.owner.name} 
                span #{video.meta.views} 회 