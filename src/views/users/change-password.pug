extends ../base

block content
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(name="oldPassword" required placeholder="old Password" type="password") 
        input(name="newPassword" required placeholder="new Password" type="password") 
        input(name="newPasswordConform" required placeholder="new Password Conform" type="password") 
        input(type="submit" value="change password")